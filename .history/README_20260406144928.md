# LeadFlow CRM

> CRM de gestão de leads com integração nativa ao WhatsApp e qualificação automática por IA.

**Versão atual:** `v1.2.0`

---

## Visão Geral

LeadFlow é uma plataforma SaaS de CRM voltada para pequenas empresas, consultores e agências que precisam gerenciar leads capturados via WhatsApp. Toda a comunicação acontece dentro da plataforma — sem precisar abrir o celular.

### O que já funciona hoje

| Módulo | Status |
|--------|--------|
| Autenticação (JWT + Refresh Token) | ✅ Produção |
| Gestão de usuários com 3 níveis de acesso | ✅ Produção |
| Pipeline de vendas Kanban customizável | ✅ Produção |
| Dashboard com métricas e KPIs | ✅ Produção |
| WhatsApp multi-conexão via Baileys | ✅ Produção |
| Chat em tempo real (SSE) | ✅ Produção |
| Qualificação automática de leads por IA | ✅ Produção |
| Multi-provedor de IA (OpenAI, Anthropic, Gemini, Grok, DeepSeek) | ✅ Produção |
| Sistema de atendentes com atribuição e transferência | ✅ Produção |
| Timeline de interações por lead | ✅ Produção |
| Anotações por lead (com permissões por criador) | ✅ Produção |
| Follow-up agendado com templates | ✅ Produção |
| Templates de mensagens | ✅ Produção |
| Automações de workflow | ✅ Produção |
| Chat interno entre membros da equipe | ✅ Produção |
| Notificações em tempo real | ✅ Produção |
| Relatórios e gráficos de performance | ✅ Produção |
| Tags e etiquetas de leads | ✅ Produção |
| LGPD — consentimento e rastreabilidade | ✅ Produção |
| Integração com N8N (automação externa) | ✅ Produção |
| Webhooks para integrações externas | ✅ Produção |
| Logs de auditoria | ✅ Produção |
| Tema claro/escuro | ✅ Produção |
| Sistema de Agentes IA | 🔜 Em desenvolvimento |

---

## Stack Tecnológica

### Frontend
- **Next.js 14** — App Router, Server Components, API Routes
- **React 18** — com hooks e SSE para tempo real
- **TypeScript 5** — tipagem estrita
- **Tailwind CSS 3** — utility-first styling
- **shadcn/ui** — componentes de UI acessíveis (Radix UI)
- **Recharts** — gráficos e visualizações
- **React Hook Form + Zod** — formulários com validação

### Backend
- **Next.js API Routes** — endpoints REST integrados
- **Prisma 5** — ORM com migrations
- **SQLite** — banco de dados local (via `prisma` com `better-sqlite3`)
- **JWT + bcrypt** — autenticação segura com refresh tokens

### WhatsApp
- **@whiskeysockets/baileys v7** — conexão direta ao WhatsApp Web (sem Evolution API)
- **Sessões persistidas** em `globalThis` (Map de sockets ativos)
- **QR Code** para autenticação de novas conexões
- **Multi-conexão** — suporte a múltiplos números simultâneos

### IA
- Módulo unificado `src/lib/ai-provider.ts` com suporte a:
  - **OpenAI** (GPT-4o, GPT-4o-mini, etc.)
  - **Anthropic Claude** (claude-sonnet-4-6, claude-opus-4-6, etc.)
  - **Google Gemini** (gemini-2.0-flash, gemini-1.5-pro, etc.)
  - **xAI Grok** (grok-3, grok-3-mini, etc.)
  - **DeepSeek** (deepseek-chat, deepseek-reasoner, etc.)
- Provedor ativo configurável em **Configurações → Integrações** sem reiniciar o servidor
- Funções: qualificação de leads, geração de respostas, sumarização de conversas

### Automação
- **N8N** — workflows externos via webhook (URL configurável)

---

## Estrutura do Projeto

```
leadflow/
├── prisma/
│   ├── schema.prisma          # Modelos do banco de dados
│   └── seed.ts                # Dados iniciais
├── src/
│   ├── app/
│   │   ├── (auth)/            # Login, cadastro, recuperação de senha
│   │   ├── (dashboard)/       # Páginas autenticadas
│   │   │   ├── page.tsx       # Dashboard principal
│   │   │   ├── leads/         # Gestão de leads + Kanban
│   │   │   ├── whatsapp/      # Chat WhatsApp em tempo real
│   │   │   ├── pipeline/      # Configuração do pipeline
│   │   │   ├── templates/     # Templates de mensagens
│   │   │   ├── automacoes/    # Automações de workflow
│   │   │   ├── relatorios/    # Relatórios e analytics
│   │   │   ├── equipe/        # Gestão de usuários
│   │   │   ├── configuracoes/ # Configurações gerais e integrações
│   │   │   └── perfil/        # Perfil do usuário
│   │   └── api/               # API Routes (REST)
│   │       ├── auth/          # Autenticação
│   │       ├── leads/         # CRUD de leads + timeline + notas
│   │       ├── whatsapp/      # Conexões, mensagens, SSE
│   │       ├── follow-ups/    # Follow-up scheduler
│   │       ├── templates/     # Templates
│   │       ├── automations/   # Automações
│   │       └── settings/      # Configurações
│   ├── lib/
│   │   ├── ai-provider.ts     # Módulo unificado de IA (5 provedores)
│   │   ├── openai.ts          # Funções de qualificação/resposta IA
│   │   ├── baileys.ts         # Gerenciador de conexões WhatsApp
│   │   ├── automation-engine.ts # Motor de automação N8N
│   │   └── prisma.ts          # Cliente Prisma singleton
│   └── components/
│       └── ui/                # Componentes shadcn/ui
└── infra/
    └── database/              # Configurações de banco
```

---

## Modelos de Dados Principais

| Modelo | Descrição |
|--------|-----------|
| `User` | Usuários com roles: `super_admin`, `admin`, `user` |
| `Lead` | Lead com score, qualificação, pipeline stage, lifecycle |
| `Conversation` | Conversa WhatsApp com status: `open`, `waiting`, `resolved`, `closed` |
| `Message` | Mensagens de conversas (incoming/outgoing, todos os tipos de mídia) |
| `WhatsappConnection` | Conexão Baileys com atendente padrão configurável |
| `Interaction` | Timeline de eventos: `note`, `pipeline_move`, `assignment`, `transfer`, `follow_up_sent` |
| `ConversationTransfer` | Auditoria de transferências entre atendentes |
| `AiAnalysis` | Resultado da análise IA por lead/conversa |
| `FollowUp` | Follow-up agendado com status scheduler |
| `MessageTemplate` | Templates reutilizáveis |
| `Setting` | Configurações chave-valor (provedores IA, atendentes, etc.) |
| `Tag` / `LeadTag` | Sistema de etiquetas com rastreabilidade |
| `AuditLog` | Log de auditoria de todas as ações |

---

## Instalação e Configuração

### Pré-requisitos
- Node.js 18+
- npm ou yarn

### 1. Instalar dependências

```bash
npm install
```

### 2. Configurar variáveis de ambiente

Crie um arquivo `.env` na raiz:

```env
# Banco de dados
DATABASE_URL="file:./dev.db"

# Autenticação
JWT_SECRET="seu-secret-jwt-aqui"
JWT_REFRESH_SECRET="seu-refresh-secret-aqui"

# IA (opcional — pode configurar pela interface)
OPENAI_API_KEY=""
ANTHROPIC_API_KEY=""
GEMINI_API_KEY=""
GROK_API_KEY=""
DEEPSEEK_API_KEY=""

# N8N (opcional)
N8N_URL=""
N8N_API_KEY=""

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"
```

### 3. Inicializar banco de dados

```bash
npm run db:push       # Cria as tabelas
npm run db:seed       # Insere dados iniciais (usuário super_admin)
```

### 4. Rodar em desenvolvimento

```bash
npm run dev
```

Acesse: `http://localhost:3000`

**Login padrão após seed:**
- Email: `admin@leadflow.com`
- Senha: `admin123`

---

## Comandos Úteis

```bash
npm run dev              # Servidor de desenvolvimento (porta 3000)
npm run build            # Build de produção
npm run start            # Iniciar build de produção
npm run db:generate      # Regenerar cliente Prisma (após mudar schema)
npm run db:push          # Sincronizar schema sem migration
npm run db:migrate       # Criar migration versionada
npm run db:seed          # Resetar e popular banco com dados iniciais
npm run db:studio        # Abrir Prisma Studio (visualizador do banco)
npm run test:e2e         # Rodar testes E2E (Playwright)
npm run test:e2e:ui      # Playwright em modo visual
```

---

## Funcionalidades em Destaque

### WhatsApp Multi-conexão
- Múltiplos números conectados simultaneamente
- Autenticação via QR Code na interface
- Reconexão automática em caso de queda
- Suporte a mensagens de texto, imagem, áudio, vídeo, documentos, stickers
- Atendente padrão configurável por conexão

### Sistema de Atendentes
- Atribuição manual de conversas a atendentes
- Transferência com registro de auditoria (`ConversationTransfer`)
- Badge "Em espera" para conversas sem atendente atribuído (`status = "waiting"`)
- Histórico de atribuições na timeline do lead

### Timeline de Lead
- Registro automático de eventos: movimentações de pipeline, atribuições, transferências, follow-ups enviados
- Anotações manuais com data, hora e autor
- Edição/exclusão permitida apenas ao criador ou `super_admin`
- Filtros por tipo de evento

### IA Multi-provedor
- Troca de provedor em tempo real via configurações (sem reiniciar servidor)
- Qualificação automática: score 0-100, classificação quente/morno/frio
- Análise de sentimento, extração de orçamento/prazo/necessidades
- Geração de sugestão de resposta contextualizada
- Sumarização de conversas longas

---

## Versionamento

| Versão | Data | Destaques |
|--------|------|-----------|
| v1.0.0 | Mar/2026 | CRM base, autenticação, pipeline, Kanban, dashboard |
| v1.1.0 | Mar/2026 | WhatsApp via Baileys, chat em tempo real, qualificação IA, follow-up, templates |
| v1.2.0 | Abr/2026 | Sistema de atendentes, transferência, timeline de lead, anotações, multi-provedor IA (Anthropic/Gemini/Grok/DeepSeek) |
| v1.3.0 | *Em desenvolvimento* | Sistema de Agentes IA com RAG, orquestração e voz |

---

## Roadmap

Veja [PROJECT_CHECKLIST.md](PROJECT_CHECKLIST.md) para o checklist detalhado de todas as fases, incluindo o sistema de Agentes IA planejado para `v1.3.0`.

---

## Licença

Privado — uso interno.
